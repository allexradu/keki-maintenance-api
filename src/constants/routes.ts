export enum ROUTES {
    // --- DOCS ---
    PUBLIC_DOCS = '/public/docs',
    PUBLIC_DEV_DOC = '/public/dev-doc',
    PUBLIC_DOC = '/public/doc',

    // --- HOME ---
    PUBLIC_HOME = '/',

    // --- FAVICON ---
    PUBLIC_FAVICON = '/favicon.ico',

    // --- MAINTENANCE ---
    PUBLIC_MAINTENANCE = '/public/maintenance',
}

export enum METHODS {
    GET = 'get',
    POST = 'post',
    PATCH = 'patch',
    DELETE = 'delete',
}
